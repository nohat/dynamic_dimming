#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/.."
mkdir -p config
export PYTHONPATH="${PYTHONPATH}:${PWD}/custom_components"
if [ ! -f config/configuration.yaml ]; then
  echo "config/configuration.yaml missing" >&2; exit 1
fi
./.venv/bin/hass --config config --debug
